# Web Framework
fastapi==0.135.1
uvicorn==0.41.0
python-multipart==0.0.22

# Pydantic (config & validation)
pydantic==2.12.5
pydantic-settings==2.13.1

# LangChain AI Framework
langchain-core==1.2.17
langchain-anthropic==1.3.4
langchain-text-splitters==1.1.1

# Vector Database
pymilvus==2.6.9
milvus-lite==2.5.1

# Embeddings (local BGE-M3 model)
FlagEmbedding==1.3.5
modelscope==1.34.0
sentence-transformers==5.2.3
torch==2.10.0
transformers==4.44.2
accelerate==1.13.0

# Web Search (optional — requires TAVILY_API_KEY)
tavily-python==0.7.22

# Re-ranking (optional — stub only, not active)
# cohere

# PDF parsing (for RAG document ingestion)
# pymupdf: uses MuPDF C library — handles Chinese CID fonts correctly.
# pdfplumber (pdfminer.six) returns \u0001 placeholders for unmapped CID characters;
# PyMuPDF decodes them correctly. Also no dependency conflicts with FlagEmbedding.
pymupdf==1.27.2
# OCR fallback for scanned/image-based PDFs (requires system tesseract):
#   brew install tesseract tesseract-lang   (macOS)
# pytesseract: Python wrapper around tesseract CLI
# pillow: image library — PyMuPDF renders a page to PNG, Pillow opens it for tesseract
pytesseract
pillow
# NOTE: unstructured was tried but abandoned — it eagerly imports unstructured_inference
#       which forces transformers → v5.x, breaking FlagEmbedding (needs v4.44.2).
# NOTE: pdfplumber was tried but abandoned — pdfminer.six fails to decode Chinese CID
#       fonts, returning \u0001 control characters instead of actual text.

# Testing
pytest
